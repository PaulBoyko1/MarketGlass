{
  "name": "marketglass",
  "version": "1.0.0",
  "private": true,
  "description": "A local-first market-state microscope, options laboratory, and hypothesis-testing environment.",
  "type": "module",
  "engines": {
    "node": ">=20"
  },
  "scripts": {
    "dev": "node src/server.mjs",
    "start": "node src/server.mjs",
    "check": "node scripts/check.mjs",
    "build": "node scripts/build.mjs",
    "test": "node --test",
    "test:browser": "playwright test",
    "test:all": "npm run check && npm test && npm run build && npm run test:browser"
  },
  "dependencies": {
    "three": "^0.180.0"
  },
  "devDependencies": {
    "@playwright/test": "^1.55.0"
  }
}
